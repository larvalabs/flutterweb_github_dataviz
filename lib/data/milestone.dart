class Milestone {
  DateTime time;
  double position;
  String label;

  // todo - time may not be necessary, position is just the position on the graph from 0..1
  Milestone(this.time, this.position, this.label);

}